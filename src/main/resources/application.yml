management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
  server:
    health:
      show-components: always
    port: 18080
  endpoints:
    web:
      exposure:
        include: "*"

data:
  origin:
    path: ${ORIGIN_DATA_PATH}