management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
  server:
    port: 18088
    health:
      show-components: always
  endpoints:
    web:
      exposure:
        include: "*"

data:
  origin:
    path: ${ORIGIN_DATA_PATH}